---
- name: Read data from CSV file
  host: all
  become: true
  tasks:
    - name: reading the csv file
      read_csv:
        path: file/data.csv
      delegate_to: localhost
      register: data_list

    - name: display CSV data
      debug:
        msg: "{{ data_list }}"
        